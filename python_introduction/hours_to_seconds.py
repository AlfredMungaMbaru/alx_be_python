#!/usr/bin/env python3
"""
Hours to Seconds Converter
This script converts hours to seconds using the formula: seconds = hours × 3600
"""

# Define variables
hours = 2  # Number of hours to convert

# Calculate seconds (1 hour = 3600 seconds)
seconds = hours * 3600

# Print the result
print(f"{hours} hour(s) is {seconds} seconds.")